# Underlay configuration as code

Main Ansible playbook for deployment of the underlay is in the [deploy.yml](./deploy.yml) file.   

Inventory is fetched from Netbox running on localhost ( for now ) with [inventory/netbox_inv.yml](./inventory/netbox_inv.yml).   
